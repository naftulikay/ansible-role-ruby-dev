---
command:
  # executing rbenv version should return the specified python version
  'sudo -Hiu {{.Env.ruby_user}} {{ .Env.rbenv_executable }} version':
    exit-status: 0
    stdout:
      - "{{.Env.ruby_version}}"
